import { StyleSheet, Dimensions } from "react-native";

export const styles = StyleSheet.create({
    button: {
      paddingTop: '1%',
      paddingBottom: '1%',
    },
    buttonText: {
      fontSize: 20,
      textAlign: 'center',   
      fontWeight: 'bold'
    },
    disabledButton: {
      opacity: 0.5,  
    }
  });